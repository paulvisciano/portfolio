{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Paul Visciano — personal portfolio site",
  "scripts": {
    "start": "lsof -ti :8000 | xargs kill 2>/dev/null; nohup live-server --port=8000 --open=\"/index.html\" > /tmp/portfolio-live-server.log 2>&1 & sleep 2 && curl -sf -o /dev/null http://127.0.0.1:8000 && echo 'Site running at http://127.0.0.1:8000'",
    "stop": "lsof -ti :8000 | xargs kill 2>/dev/null; echo 'Server stopped'"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}